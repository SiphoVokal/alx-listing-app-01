// API URLs
export const API_BASE_URL = "https://api.example.com";

// UI Strings
export const UI_TEXT = {
  bookNow: "Book Now",
  viewDetails: "View Details",
  noProperties: "No properties available at the moment.",
};

// Configuration
export const CONFIG = {
  currency: "ZAR",
  defaultLocale: "en-RSA",
};
